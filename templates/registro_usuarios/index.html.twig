{% extends 'base.html.twig' %}

{% block title %}Registro Usuarios{% endblock %}

{% block body %}

<div class="example-wrapper">
    <h1>Bienvenido al {{ controller_name }}! ✅</h1>

    {% for message in app.flashes('exito') %} //llamamos a la clave de acceso "exito"
        <div class="alert alert-success">
            {{ message }}
        </div>
    {% endfor %}
    Ingrese Credenciales del Usuario
    {{ form(formulario) }}

</div>
{% endblock %}
